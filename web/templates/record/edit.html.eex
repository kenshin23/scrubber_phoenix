<h2>Edit record</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_document_record_path(@conn, :update, @user, @document, @record) %>

<%= link "Back", to: user_document_record_path(@conn, :index, @user, @document) %>